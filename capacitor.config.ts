import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.serg.app',
  appName: 'apiconnection',
  webDir: 'dist/apiconnection',
  bundledWebRuntime: false
};

export default config;
